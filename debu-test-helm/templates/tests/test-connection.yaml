apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "debu-test-helm.fullname" . }}-test-connection"
  labels:
    {{- include "debu-test-helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "debu-test-helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
